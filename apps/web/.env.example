# AI Chat Platform Environment Variables

# OpenRouter API Key (get it from https://openrouter.ai/keys)
# Without this key, the app will use mock responses
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# App URL (used for OpenRouter referer header)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# JWT Secret for authentication (generate a random string)
JWT_SECRET=your-jwt-secret-here-min-32-chars-long
