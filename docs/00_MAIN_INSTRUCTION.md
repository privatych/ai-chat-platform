# 🚀 AI Chat Platform - Полная инструкция для Claude Code

## 📖 Структура документации

Эта документация разделена на несколько файлов для удобства навигации:

1. **00_MAIN_INSTRUCTION.md** (этот файл) - Общий обзор и навигация
2. **01_PROJECT_DISCOVERY.md** - Вопросы для уточнения перед началом
3. **02_ARCHITECTURE.md** - Архитектура и технический стек
4. **03_PHASE_1_BACKEND.md** - Фаза 1: Backend разработка (3-4 дня)
5. **04_PHASE_2_WEB_FRONTEND.md** - Фаза 2: Web Frontend (5-6 дней)
6. **05_PHASE_3_MOBILE.md** - Фаза 3: Mobile приложение (3-4 дня)
7. **06_PHASE_4_PAYMENTS.md** - Фаза 4: Платежи и подписки (2-3 дня)
8. **07_DEPLOYMENT.md** - Деплой и CI/CD
9. **08_OPTIMIZATION.md** - Оптимизация и бюджет
10. **09_TESTING.md** - Тестирование
11. **10_CHECKLIST.md** - Финальный чек-лист

---

## 🎯 Цель проекта

Создать с нуля **мультимодельную AI-чат платформу** с минимальным бюджетом и максимальной скоростью запуска.

### Ключевые характеристики:

✅ **Мультиплатформенность**: Web + iOS + Android  
✅ **Мультимодельность**: Доступ к 20+ AI моделям через единый интерфейс  
✅ **Монетизация**: Freemium модель с подписками  
✅ **Скорость**: MVP за 6-8 недель  
✅ **Бюджет**: Минимальный (от $5-10/месяц)  

---

## 📊 Обзор технологического стека

```
┌─────────────────────────────────────────────────────────┐
│                    КЛИЕНТСКИЕ ПРИЛОЖЕНИЯ                 │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐              ┌───────────────────┐   │
│  │   Web App    │              │   Mobile App      │   │
│  │  (Next.js)   │              │  (React Native)   │   │
│  └──────────────┘              └───────────────────┘   │
│                                                          │
├─────────────────────────────────────────────────────────┤
│                      BACKEND (Fastify)                   │
│  • REST API • JWT Auth • Rate Limiting • SSE Streaming   │
├─────────────────────────────────────────────────────────┤
│         DATABASE                │      EXTERNAL APIS    │
│  • PostgreSQL (Neon/Railway)    │  • OpenRouter         │
│  • Redis (Upstash)              │  • YooKassa           │
│  • Drizzle ORM                  │  • Sentry, PostHog    │
└─────────────────────────────────────────────────────────┘
```

### Основные технологии:

**Frontend (Web):**
- Next.js 15 (App Router)
- React 19
- TypeScript
- Tailwind CSS + shadcn/ui
- TanStack Query (React Query)
- Zustand

**Mobile:**
- React Native (Expo SDK 52)
- Expo Router
- NativeWind (Tailwind для RN)
- Zustand + TanStack Query

**Backend:**
- Fastify (Node.js)
- Drizzle ORM
- PostgreSQL 15
- Redis 7
- JWT Authentication

**Infrastructure:**
- Monorepo: Turborepo
- Package Manager: pnpm
- CI/CD: GitHub Actions
- Hosting: Vercel (web) + Railway (API)

---

## 🗺️ Дорожная карта разработки

### MVP (6-8 недель)

**Неделя 1-2: Backend Foundation**
- ✅ Database schemas (Drizzle ORM)
- ✅ Fastify API setup
- ✅ Authentication (JWT)
- ✅ OpenRouter integration
- ✅ Basic chat endpoints

**Неделя 3-4: Web Frontend**
- ✅ Next.js app structure
- ✅ Authentication UI
- ✅ Chat interface
- ✅ Model selection
- ✅ Chat history

**Неделя 5-6: Mobile App**
- ✅ Expo setup
- ✅ Authentication screens
- ✅ Chat interface
- ✅ Sync with backend

**Неделя 7-8: Polish & Deploy**
- ✅ YooKassa integration (payments)
- ✅ Rate limiting & quotas
- ✅ Error handling
- ✅ Testing
- ✅ Deploy to production

### Post-MVP (v1.0)

**Week 9-12:**
- Premium features (comparison mode, export)
- Advanced model parameters
- Email notifications
- Analytics dashboard
- Performance optimization

---

## 🚀 Быстрый старт для Claude Code

### Шаг 1: Прочитать вопросы для уточнения

```bash
# Открыть файл с вопросами
cat 01_PROJECT_DISCOVERY.md
```

**Действие:** Ответь на все вопросы в файле. Это критически важно для правильной настройки проекта.

### Шаг 2: Изучить архитектуру

```bash
# Прочитать архитектурную документацию
cat 02_ARCHITECTURE.md
```

**Действие:** Понять структуру проекта, data flow, и принятые архитектурные решения.

### Шаг 3: Начать с Backend

```bash
# Перейти к фазе 1
cat 03_PHASE_1_BACKEND.md
```

**Действие:** Следовать пошаговым инструкциям для создания backend. Каждая задача содержит:
- Описание задачи
- Код примеры
- Acceptance criteria для проверки

### Шаг 4: Продолжить согласно плану

Последовательно выполнять фазы 2, 3, 4 из соответствующих файлов.

---

## 💡 Рекомендации для Claude Code

### Принципы работы:

1. **Последовательность**: Выполняй фазы по порядку. Не перескакивай.
2. **Проверка**: После каждой задачи проверяй Acceptance Criteria.
3. **Тестирование**: Запускай проект на каждом этапе, проверяй работоспособность.
4. **Документация**: Обновляй README при добавлении новых фич.
5. **Git commits**: Делай понятные коммиты после каждой завершенной задачи.

### Приоритеты:

1. **Скорость > Совершенство**: Для MVP достаточно "работает", не "идеально"
2. **Простота > Сложность**: Избегай overengineering
3. **MVP-first**: Откладывай не критичные фичи на v1.0/v2.0
4. **Budget-conscious**: Всегда выбирай free tier где возможно

### Когда задавать уточняющие вопросы:

- ❓ Если в задаче неясны требования
- ❓ Если нужно выбрать между несколькими подходами
- ❓ Если встречаешь технические ограничения
- ❓ Если задача требует больше времени чем указано

**НЕ спрашивай о:**
- ✋ Базовом синтаксисе (используй документацию)
- ✋ Очевидных implementation деталях
- ✋ Стилистике кода (следуй примерам)

---

## 📝 Формат работы с задачами

Каждая задача в фазах представлена в следующем формате:

```markdown
#### X.X Название задачи

**Задача для Claude Code:**

"[Детальное описание что нужно сделать]"

**Требования:**
- Требование 1
- Требование 2
- ...

**Пример кода:**
[Код пример для reference]

**Acceptance Criteria:**
- [ ] Критерий 1
- [ ] Критерий 2
- [ ] ...
```

### Как работать с задачей:

1. **Читай описание** полностью
2. **Изучи требования** внимательно
3. **Посмотри пример кода** (если есть)
4. **Напиши implementation**
5. **Протестируй** работу
6. **Проверь Acceptance Criteria** - все ли пункты выполнены
7. **Сделай commit** с осмысленным сообщением

### Пример commit message:

```bash
git commit -m "feat(auth): implement login and register endpoints

- Add JWT token generation
- Add password hashing with bcrypt
- Add input validation with Zod
- Add unit tests for auth routes

Closes #1"
```

---

## 🔄 Workflow для Claude Code

### Типичный цикл работы:

```
1. Read task → 
2. Understand requirements → 
3. Write code → 
4. Test locally → 
5. Check acceptance criteria → 
6. Commit → 
7. Move to next task
```

### Если что-то не работает:

1. **Прочитай error message** внимательно
2. **Проверь configuration** (env variables, package.json)
3. **Проверь dependencies** (pnpm install выполнен?)
4. **Проверь документацию** библиотеки
5. **Задай вопрос** если застрял >30 минут

### Testing checklist:

После каждой значительной фичи:

- [ ] Код компилируется без TypeScript errors
- [ ] ESLint не выдает критичных warnings
- [ ] Dev server запускается без errors
- [ ] Функционал работает в браузере/симуляторе
- [ ] Unit tests (если есть) проходят

---

## 📚 Полезные ресурсы

### Документация:

- **Next.js**: https://nextjs.org/docs
- **React Native**: https://reactnative.dev/docs
- **Expo**: https://docs.expo.dev/
- **Fastify**: https://fastify.dev/docs/
- **Drizzle ORM**: https://orm.drizzle.team/docs/
- **TailwindCSS**: https://tailwindcss.com/docs
- **shadcn/ui**: https://ui.shadcn.com/

### API References:

- **OpenRouter**: https://openrouter.ai/docs
- **YooKassa**: https://yookassa.ru/developers
- **Expo Router**: https://docs.expo.dev/router/introduction/

### Tools:

- **Drizzle Studio**: Визуальный редактор БД (запуск: `pnpm db:studio`)
- **React Query Devtools**: Отладка server state
- **Expo Go**: Тестирование mobile app на устройстве

---

## ⚠️ Важные напоминания

### КРИТИЧНО для успеха проекта:

1. **Environment Variables**: Всегда проверяй что .env файлы настроены правильно
2. **API Keys**: Никогда не коммить API keys в Git
3. **Database Migrations**: Всегда создавай и применяй миграции при изменении schema
4. **Type Safety**: Используй TypeScript строго, избегай `any`
5. **Error Handling**: Каждый async operation должен иметь try/catch
6. **User Data**: Никогда не логируй sensitive data (passwords, tokens)

### Частые ошибки (избегай):

❌ Забыть установить dependencies (`pnpm install`)  
❌ Не обновить .env после добавления переменных  
❌ Коммитить .env файлы  
❌ Игнорировать TypeScript errors  
❌ Не тестировать перед commit  
❌ Overengineering для MVP  
❌ Добавлять фичи не из плана  

---

## 🎓 Обучающие примеры

### Пример 1: Как создать новый API endpoint

**Задача**: Добавить endpoint для получения информации о пользователе

**Шаги:**

1. Создать route файл:
```typescript
// services/api/src/routes/users/me.ts
import { FastifyRequest, FastifyReply } from 'fastify';

export async function getMe(request: FastifyRequest, reply: FastifyReply) {
  const userId = request.user.userId; // From JWT middleware
  
  // Get user from DB
  const user = await db.query.users.findFirst({
    where: (users, { eq }) => eq(users.id, userId),
  });
  
  if (!user) {
    return reply.code(404).send({ error: 'User not found' });
  }
  
  return reply.send({
    id: user.id,
    email: user.email,
    fullName: user.fullName,
    subscriptionTier: user.subscriptionTier,
  });
}
```

2. Зарегистрировать route:
```typescript
// services/api/src/routes/users/index.ts
import { FastifyInstance } from 'fastify';
import { getMe } from './me';

export default async function userRoutes(app: FastifyInstance) {
  app.get('/me', { onRequest: [app.authenticate] }, getMe);
}
```

3. Добавить в главный app:
```typescript
// services/api/src/app.ts
import userRoutes from './routes/users';

app.register(userRoutes, { prefix: '/api/users' });
```

4. Протестировать:
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:3001/api/users/me
```

### Пример 2: Добавление UI компонента

**Задача**: Создать кнопку с loading state

```typescript
// components/ui/LoadingButton.tsx
import { Button, ButtonProps } from '@/components/ui/button';
import { Loader2 } from 'lucide-react';

interface LoadingButtonProps extends ButtonProps {
  isLoading?: boolean;
}

export function LoadingButton({ 
  isLoading, 
  children, 
  disabled,
  ...props 
}: LoadingButtonProps) {
  return (
    <Button disabled={isLoading || disabled} {...props}>
      {isLoading && <Loader2 className="mr-2 h-4 w-4 animate-spin" />}
      {children}
    </Button>
  );
}

// Usage:
<LoadingButton isLoading={isSubmitting} onClick={handleSubmit}>
  Submit
</LoadingButton>
```

---

## 📞 Что делать если нужна помощь

### Перед тем как спросить:

1. ✅ Прочитал ли я документацию?
2. ✅ Пробовал ли я погуглить error message?
3. ✅ Проверил ли я примеры кода в этой инструкции?
4. ✅ Застрял ли я больше чем на 30 минут?

### Формат вопроса:

```
**Контекст**: Работаю над [название задачи]

**Проблема**: [Описание проблемы]

**Что я уже пробовал**:
1. [Попытка 1]
2. [Попытка 2]

**Error message** (если есть):
```
[Error text]
```

**Вопрос**: [Конкретный вопрос]
```

---

## ✅ Готов начать?

### Контрольный список перед стартом:

- [ ] Прочитал весь файл 00_MAIN_INSTRUCTION.md
- [ ] Понимаю цели проекта
- [ ] Знаю структуру документации
- [ ] Готов отвечать на вопросы в 01_PROJECT_DISCOVERY.md
- [ ] Установлены необходимые инструменты:
  - [ ] Node.js 20+
  - [ ] pnpm 9+
  - [ ] Git
  - [ ] VS Code (или другой IDE)
  - [ ] Docker (для PostgreSQL/Redis локально)

### Следующий шаг:

👉 **Перейти к файлу `01_PROJECT_DISCOVERY.md`** и ответить на все вопросы.

После ответов на вопросы, вернись сюда и переходи к фазе 1:

```bash
cat 03_PHASE_1_BACKEND.md
```

---

## 📊 Метрики успеха MVP

### Технические метрики:

- ✅ 0 TypeScript errors
- ✅ 0 критичных ESLint warnings
- ✅ >70% test coverage (для критичных путей)
- ✅ API response time <500ms (95th percentile)
- ✅ Web app Lighthouse score >90
- ✅ Mobile app запускается на iOS и Android

### Функциональные метрики:

- ✅ Пользователь может зарегистрироваться
- ✅ Пользователь может создать чат и получить ответ от AI
- ✅ Пользователь может переключать между моделями
- ✅ Пользователь может видеть историю чатов
- ✅ Free tier rate limiting работает
- ✅ Premium подписка может быть оплачена

### Бизнес метрики (post-launch):

- Daily Active Users (DAU)
- Messages sent per user
- Conversion rate (free → premium)
- Churn rate

---

## 🎉 Удачи!

Следуй инструкциям последовательно, не торопись, тестируй на каждом шаге, и у тебя получится отличный продукт!

**Помни**: Лучше "shipped and imperfect" чем "perfect and never launched".

---

_Последнее обновление: 2026-01-27_
_Версия: 1.0.0_
