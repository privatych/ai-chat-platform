# План улучшений и оптимизации AI Chat Platform

## 🔴 Критические проблемы

### 1. TypeScript ошибки
**Проблема:** Есть несколько TypeScript ошибок компиляции
- `apps/web/components/theme-provider.tsx` - неправильный импорт типов из `next-themes`
- `apps/web/components/user-menu.tsx` - тип `subscriptionExpiresAt` может быть `undefined`
- `services/api` - конфликт версий `drizzle-orm` (0.30.10 vs 0.45.1)

**Приоритет:** HIGH
**Время:** 1-2 часа

**Решение:**
```bash
# Обновить зависимости до одной версии
pnpm update drizzle-orm

# Исправить типы
user.subscriptionExpiresAt ? formatDate(user.subscriptionExpiresAt) : 'Неактивна'
```

### 2. Отсутствие обработки ошибок
**Проблема:** Многие API вызовы не обрабатывают ошибки должным образом

**Примеры:**
```typescript
// apps/web/components/projects/ProjectSelector.tsx:66
const loadProjects = async () => {
  const response = await apiClient.listProjects();
  if (response.success && response.data) {
    setProjects(response.data);
  }
  // ❌ Нет обработки ошибок!
};
```

**Приоритет:** HIGH
**Время:** 2-3 часа

**Решение:**
```typescript
const loadProjects = async () => {
  try {
    const response = await apiClient.listProjects();
    if (response.success && response.data) {
      setProjects(response.data);
    } else {
      toast.error('Не удалось загрузить проекты');
    }
  } catch (error) {
    console.error('Failed to load projects:', error);
    toast.error('Ошибка при загрузке проектов');
  }
};
```

### 3. Слабая типизация
**Проблема:** Используется `any` вместо конкретных типов

**Примеры:**
- `chats: any[]` - должен быть `Chat[]`
- `projects: any[]` - должен быть `Project[]`
- `selectedChat: any` - должен быть `Chat | null`

**Приоритет:** MEDIUM
**Время:** 3-4 часа

**Решение:**
```typescript
// Создать файл types/chat.ts
export interface Chat {
  id: string;
  userId: string;
  title: string;
  model: string;
  projectId: string | null;
  project?: {
    id: string;
    name: string;
    description: string | null;
  };
  systemPrompt: string | null;
  temperature: number | null;
  useProjectContext: boolean;
  createdAt: Date;
  updatedAt: Date;
}
```

## 🟡 Важные улучшения

### 4. Безопасность
**Проблемы:**
- Нет rate limiting на API endpoints
- Нет валидации размера файлов при загрузке
- Нет защиты от CSRF
- JWT_SECRET может быть не установлен (есть проверка, но нет дефолта для dev)

**Приоритет:** HIGH
**Время:** 4-5 часов

**Решение:**
```typescript
// Rate limiting
import rateLimit from '@fastify/rate-limit';

fastify.register(rateLimit, {
  max: 100,
  timeWindow: '15 minutes'
});

// File size validation
const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB
if (file.size > MAX_FILE_SIZE) {
  throw new Error('File too large');
}
```

### 5. Производительность загрузки чатов
**Проблема:**
- Загружается только 50 чатов, нет пагинации
- Нет виртуализации списка (все рендерятся сразу)
- Нет кеширования

**Приоритет:** MEDIUM
**Время:** 5-6 часов

**Решение:**
```typescript
// Добавить cursor-based pagination
interface GetChatsParams {
  limit?: number;
  cursor?: string; // последний chatId
}

// Использовать react-window для виртуализации
import { FixedSizeList } from 'react-window';
```

### 6. Оптимизация запросов к БД
**Проблемы:**
- При создании чата делается 2 запроса (insert + select)
- Нет индексов на projectId в chats таблице
- Нет prepared statements

**Приоритет:** MEDIUM
**Время:** 2-3 часа

**Решение:**
```typescript
// Использовать returning с leftJoin (Drizzle ORM не поддерживает напрямую)
// Альтернатива: использовать raw SQL или добавить индексы

// migration
CREATE INDEX idx_chats_project_id ON chats(project_id);
CREATE INDEX idx_chats_user_id_updated_at ON chats(user_id, updated_at DESC);
```

### 7. Состояние загрузки
**Проблема:** Нет индикаторов загрузки для многих операций

**Примеры:**
- Загрузка списка чатов
- Загрузка проектов
- Удаление чата/проекта

**Приоритет:** MEDIUM
**Время:** 2-3 часа

**Решение:**
```typescript
const [isLoading, setIsLoading] = useState(true);

useEffect(() => {
  loadChats().finally(() => setIsLoading(false));
}, []);

if (isLoading) {
  return <Spinner />;
}
```

### 8. Дебаг логи в продакшене
**Проблема:** `console.log` остаются в коде

**Файлы:**
- `apps/web/app/chat/page.tsx` - строки 46, 52, 54, 72, 74, 82

**Приоритет:** LOW
**Время:** 30 минут

**Решение:**
```typescript
// Использовать conditional logging
const isDev = process.env.NODE_ENV === 'development';
if (isDev) {
  console.log('[Create Chat] Selected project:', selectedProjectId);
}

// Или использовать logger библиотеку
import { logger } from '@/lib/logger';
logger.debug('Create chat', { projectId: selectedProjectId });
```

## 🟢 Желательные улучшения

### 9. UX улучшения

#### 9.1 Автосохранение названия чата
**Проблема:** Все чаты называются "Новый чат"

**Решение:**
```typescript
// Автоматически переименовывать чат на основе первого сообщения
const autoRenameChat = (chatId: string, firstMessage: string) => {
  const title = firstMessage.slice(0, 50);
  apiClient.renameChat(chatId, title);
};
```

#### 9.2 Подтверждение перед удалением
**Статус:** ✅ Уже реализовано (AlertDialog)

#### 9.3 Поиск по чатам
**Приоритет:** MEDIUM
**Время:** 3-4 часа

```typescript
const [searchQuery, setSearchQuery] = useState('');
const filteredChats = chats.filter(chat =>
  chat.title.toLowerCase().includes(searchQuery.toLowerCase())
);
```

#### 9.4 Горячие клавиши
**Приоритет:** LOW
**Время:** 2-3 часа

```typescript
// Ctrl+K - поиск чатов
// Ctrl+N - новый чат
// Ctrl+/ - открыть проекты
```

### 10. Функциональные улучшения

#### 10.1 Экспорт чата
**Описание:** Экспорт истории чата в Markdown/PDF

**Приоритет:** LOW
**Время:** 4-5 часов

#### 10.2 Шаринг чата
**Описание:** Публичная ссылка на чат (read-only)

**Приоритет:** LOW
**Время:** 6-8 часов

#### 10.3 Теги для чатов
**Описание:** Добавить теги вместо/вместе с проектами

**Приоритет:** LOW
**Время:** 8-10 часов

### 11. Тестирование

#### 11.1 Unit тесты
**Текущее состояние:** 0% покрытие

**Приоритет:** MEDIUM
**Время:** 10-15 часов

```bash
# Настроить Vitest
pnpm add -D vitest @testing-library/react @testing-library/jest-dom

# Тесты для API routes
# Тесты для компонентов
# Тесты для утилит
```

#### 11.2 E2E тесты
**Приоритет:** LOW
**Время:** 15-20 часов

```bash
# Настроить Playwright
pnpm add -D @playwright/test

# Основные флоу:
# - Регистрация/логин
# - Создание проекта
# - Создание чата
# - Отправка сообщения
```

### 12. Документация

#### 12.1 API документация
**Статус:** Отсутствует

**Приоритет:** MEDIUM
**Время:** 4-5 часов

**Решение:**
```bash
# Добавить Swagger/OpenAPI
pnpm add @fastify/swagger @fastify/swagger-ui
```

#### 12.2 README
**Статус:** Нужно обновить

**Содержание:**
- Описание проекта
- Как запустить локально
- Структура проекта
- Переменные окружения
- Deployment guide

#### 12.3 Комментарии в коде
**Проблема:** Сложная логика не документирована

### 13. Инфраструктура

#### 13.1 Docker setup
**Приоритет:** MEDIUM
**Время:** 3-4 часа

```dockerfile
# Dockerfile для каждого сервиса
# docker-compose.yml для локальной разработки
```

#### 13.2 CI/CD
**Приоритет:** MEDIUM
**Время:** 4-5 часов

```yaml
# GitHub Actions
# - Запуск тестов
# - Проверка типов
# - Линтинг
# - Build
# - Deploy на staging/production
```

#### 13.3 Monitoring
**Приоритет:** LOW
**Время:** 6-8 часов

```typescript
// Логирование ошибок (Sentry)
// Метрики производительности
// Uptime monitoring
```

## 📊 Оценка приоритетов

### Немедленно (1-2 недели)
1. ✅ Исправить TypeScript ошибки
2. ✅ Добавить обработку ошибок
3. ✅ Добавить строгую типизацию
4. ✅ Улучшить безопасность (rate limiting, file validation)
5. ✅ Удалить debug логи из продакшена

### Краткосрочно (1 месяц)
1. ✅ Оптимизация БД (индексы, queries)
2. ✅ Индикаторы загрузки
3. ✅ Пагинация чатов
4. ✅ Поиск по чатам
5. ✅ Unit тесты (критические части)
6. ✅ Docker setup
7. ✅ CI/CD pipeline
8. ✅ API документация

### Среднесрочно (2-3 месяца)
1. ✅ Автосохранение названий чатов
2. ✅ Горячие клавиши
3. ✅ Экспорт чатов
4. ✅ E2E тесты
5. ✅ Monitoring setup
6. ✅ Виртуализация списков

### Долгосрочно (3+ месяца)
1. ✅ Шаринг чатов
2. ✅ Система тегов
3. ✅ Расширенная аналитика
4. ✅ Мобильное приложение

## 🔧 Технический долг

### Существующие проблемы
1. Две версии drizzle-orm в проекте (конфликт)
2. Нет .gitignore для tsconfig.tsbuildinfo
3. Нет валидации env переменных при старте
4. Жестко заданные значения (magic numbers)
5. Дублирование кода в API handlers

### Рефакторинг
1. Вынести общую логику в хуки (useChat, useProject)
2. Создать UI компоненты библиотеку (shared package)
3. Нормализовать структуру стейта (возможно Zustand)
4. Разделить большие компоненты (ChatSidebar, ContextEditor)

## 📈 Метрики для отслеживания

### Производительность
- Time to First Byte (TTFB) < 200ms
- First Contentful Paint (FCP) < 1s
- Time to Interactive (TTI) < 3s
- Размер бандла < 300kb (gzipped)

### Качество кода
- TypeScript coverage: 100% (сейчас ~85%)
- Test coverage: >80% (сейчас 0%)
- Lighthouse score: >90 (Performance, Accessibility, Best Practices, SEO)

### Надежность
- Error rate < 0.1%
- Uptime > 99.9%
- Response time p95 < 500ms
