# Changelog - 2026-02-09

## 🐛 Критические исправления

### 1. Исправлена загрузка файлов (FST_ERR_CTP_BODY_TOO_LARGE)

**Проблема:**
- Невозможно загрузить PDF/изображения в контекст проектов
- Изображения в чатах выдавали ошибку "Ошибка при получении ответа"
- Ошибка: `Request body is too large (413)`

**Причина:**
- Fastify по умолчанию ограничивает размер body до 1MB
- Base64-кодированные файлы (10MB → 13.3MB) превышали лимит

**Решение:**
- Увеличен `bodyLimit` до 20MB в конфигурации Fastify
- Файл: `services/api/src/app.ts`
- Теперь поддерживаются файлы до ~15MB оригинального размера

**Коммит:** `fix(api): increase bodyLimit to 20MB for file uploads`

---

### 2. Исправлено удаление чатов (FST_ERR_CTP_EMPTY_JSON_BODY)

**Проблема:**
- При попытке удалить чат ничего не происходило
- Ошибка: `Body cannot be empty when content-type is set to 'application/json' (400)`

**Причина:**
- API client всегда добавлял заголовок `Content-Type: application/json`
- DELETE запросы не имеют body
- Fastify видел заголовок и ожидал JSON body → отклонял запрос

**Решение:**
- API client теперь не добавляет `Content-Type` для запросов без body
- Файл: `apps/web/lib/api-client.ts:26-32`
- Добавлено логирование для отладки в:
  - `apps/web/app/chat/page.tsx:68-78`
  - `services/api/src/routes/chat/delete.ts:9-36`

**Коммит:** `fix(api): remove Content-Type header for requests without body`

---

## 📋 Следующие задачи

### Из предыдущего отчета (FILE_UPLOAD_FIX_REPORT.md)

**Средний приоритет:**
- [ ] Валидация размера base64 (может превысить API лимиты)
- [ ] Progress bar для загрузки больших файлов
- [ ] PDF preview в UI

**Низкий приоритет:**
- [ ] Конфигурируемый file size limit (ENV var)
- [ ] Сжатие изображений перед отправкой
- [ ] Token-aware context limit для сообщений

### Новые задачи для проверки

- [ ] Проверить все API endpoints на наличие подобных проблем
- [ ] Протестировать загрузку различных типов файлов
- [ ] Проверить работу context injection в AI промптах
- [ ] Протестировать переименование чатов
- [ ] Проверить работу Projects CRUD операций

---

## 🧪 Как протестировать

### Загрузка файлов:
1. Откройте http://localhost:3000
2. Создайте проект или откройте существующий
3. Нажмите "Редактировать контекст"
4. Загрузите PDF файл (до 10MB)
5. ✅ Файл должен успешно загрузиться

### Удаление чатов:
1. В списке чатов выберите любой чат
2. Нажмите три точки → "Удалить"
3. Подтвердите удаление
4. ✅ Чат должен исчезнуть из списка

### Изображения в чате:
1. Откройте любой чат
2. Прикрепите изображение
3. Отправьте сообщение "Что на изображении?"
4. ✅ AI должен описать изображение

---

## 📊 Статистика

| Метрика | Значение |
|---------|----------|
| Багов исправлено | 2 |
| Файлов изменено | 3 |
| Строк кода | +15, -3 |
| Время на исправление | ~30 минут |

---

## 🔄 Серверы

- **Frontend:** http://localhost:3000
- **API:** http://localhost:3001

Оба сервера работают и готовы к тестированию.
